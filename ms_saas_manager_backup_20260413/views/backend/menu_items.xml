<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ── Root Menu ────────────────────────────────── -->
    <menuitem id="menu_ms_saas_root"
              name="SaaS Manager"
              sequence="80"
              web_icon="ms_saas_manager,static/src/img/icon.png"
              groups="ms_saas_manager.group_ms_saas_admin"/>

    <!-- ── Dashboard ────────────────────────────────── -->
    <menuitem id="menu_ms_dashboard"
              name="📊 Dashboard"
              parent="menu_ms_saas_root"
              action="action_ms_dashboard"
              sequence="1"/>

    <!-- ── Subscriptions ────────────────────────────── -->
    <menuitem id="menu_ms_subscriptions"
              name="Subscriptions"
              parent="menu_ms_saas_root"
              sequence="10"/>

    <menuitem id="menu_ms_subscription_all"
              name="All Subscriptions"
              parent="menu_ms_subscriptions"
              action="action_ms_subscription"
              sequence="1"/>

    <menuitem id="menu_ms_subscription_active"
              name="Active"
              parent="menu_ms_subscriptions"
              action="action_ms_subscription"
              sequence="2"/>

    <menuitem id="menu_ms_subscription_trial"
              name="Free Trials"
              parent="menu_ms_subscriptions"
              sequence="3"/>

    <!-- ── Databases ─────────────────────────────────── -->
    <menuitem id="menu_ms_databases"
              name="🗄️ Customer Databases"
              parent="menu_ms_saas_root"
              action="action_ms_managed_db"
              sequence="20"/>

    <!-- ── Billing ───────────────────────────────────── -->
    <menuitem id="menu_ms_billing"
              name="Billing"
              parent="menu_ms_saas_root"
              sequence="30"/>

    <menuitem id="menu_ms_invoices"
              name="Invoices"
              parent="menu_ms_billing"
              action="action_ms_invoice"
              sequence="1"/>

    <!-- ── Configuration ────────────────────────────── -->
    <menuitem id="menu_ms_config"
              name="⚙️ Configuration"
              parent="menu_ms_saas_root"
              sequence="90"/>

    <menuitem id="menu_ms_plan_templates"
              name="Plan Templates"
              parent="menu_ms_config"
              action="action_ms_plan_template"
              sequence="1"/>

</odoo>
